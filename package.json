{
  "name": "jasmine-cookies",
  "version": "1.0.0",
  "main": "built/index.js",
  "typings": "built/index.d.ts",
  "dependencies": {
    "@types/jasmine": "2.6.0",
    "@types/node": "9.6.4",
    "csv-to-deep-json": "git+ssh://git@github.com/KnowledgeExpert/csv-to-deep-json.git",
    "xlsx": "0.12.7",
    "typescript": "2.5.3"
  },
  "scripts": {
    "clean": "rm -rf ./built",
    "build": "npm run clean && tsc",
    "test": "npm run clean && npm run build && jasmine ./built/test/*.test.js"
  }
}
